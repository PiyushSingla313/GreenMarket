fastapi>=0.141.0
uvicorn[standard]>=0.52.0
SQLAlchemy>=2.0.52
python-jose[cryptography]>=3.5.0
bcrypt>=5.0.0
pydantic>=2.13.0
pydantic-settings>=2.15.0
python-multipart>=0.0.32

# Only needed if you point DATABASE_URL at PostgreSQL instead of the default
# SQLite file. Requires PostgreSQL client headers (pg_config) to build, or use
# a platform with a prebuilt wheel available.
# psycopg2-binary==2.9.9
